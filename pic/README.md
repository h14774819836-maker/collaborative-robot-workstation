# Runtime Data

This directory is kept for runtime outputs only.

Large sample data, training archives, and captured images are intentionally excluded from the repository.

Typical runtime subdirectories:

- `cam1/`
- `biaoding1/`
- `result/`
